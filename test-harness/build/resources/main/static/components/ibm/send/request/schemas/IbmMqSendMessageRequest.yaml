description: >-
  Here, due to the limitations of the Open API specification, it is displayed as
  an object; however, it is expected to be a string in JSON format representing
  the destination details.
type: object
properties:
  destinationsDetailsList:
    type: array
    items:
      $ref: ../../../../common/schemas/ibm/DestinationDetails.yaml
example:
  destinationsDetailsList:
    - server: localhost
      port: 1414
      queueManager: QM1
      destinationNames:
        - DEV.QUEUE.2
