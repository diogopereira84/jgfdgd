type: object
required:
  - header
properties:
  header:
    $ref: ./OutgoingEventHeaderDto.yaml
  payload:
    $ref: ./EventPayloadDto.yaml
  metadata:
    description: This map accepts any key and value, provided that both are strings.
    type: object
