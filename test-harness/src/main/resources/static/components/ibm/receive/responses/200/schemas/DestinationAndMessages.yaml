type: object
required:
  - server
  - port
  - messageDetailsList
properties:
  server:
    type: string
  port:
    type: integer
  messageDetailsList:
    type: array
    items:
      $ref: ../../schemas/MessageDetails.yaml
