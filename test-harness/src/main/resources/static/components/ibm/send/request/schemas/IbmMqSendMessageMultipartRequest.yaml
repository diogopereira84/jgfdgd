type: object
required:
  - request
  - messages
properties:
  request:
    $ref: ./IbmMqSendMessageRequest.yaml
  messages:
    description: >-
      The content of this file is expected to be in JSON format, containing an
      array of strings, where each string represents a message.
    type: string
    format: binary
